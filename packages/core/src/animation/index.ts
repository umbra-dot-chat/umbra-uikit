/**
 * @module animation
 * @description Barrel export for Wisp animation presets and constants.
 *
 * Re-exports declarative animation presets and motion-token aliases
 * used across all Wisp components.
 *
 * @example
 * ```ts
 * import {
 *   fadeIn,
 *   scaleIn,
 *   DEFAULT_DURATION,
 *   DEFAULT_EASING,
 * } from '@wisp-ui/core/animation';
 * ```
 */

export * from './presets';
export * from './constants';
