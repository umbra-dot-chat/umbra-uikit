/**
 * @module contexts
 * @description Barrel export for all Wisp UI kit React contexts.
 *
 * @example
 * ```tsx
 * import { LoadingContext } from '../contexts';
 *
 * <LoadingContext.Provider value={true}>
 *   <MyComponent />
 * </LoadingContext.Provider>
 * ```
 */

export { LoadingContext } from './loading-context';
