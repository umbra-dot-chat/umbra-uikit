/**
 * FileContextMenu + FolderContextMenu -- public API surface.
 *
 * @module components/file-context-menu
 */

export { FileContextMenu, FolderContextMenu } from './FileContextMenu';
export type {
  FileContextMenuProps,
  FolderContextMenuProps,
} from '@coexist/wisp-core/types/FileContextMenu.types';
