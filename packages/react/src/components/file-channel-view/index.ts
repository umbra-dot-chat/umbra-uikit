/**
 * FileChannelView component -- public API surface.
 *
 * @module components/file-channel-view
 */

export { FileChannelView } from './FileChannelView';
export type {
  FileChannelViewProps,
  FileFolder,
  FileEntry,
  FileViewMode,
  FileSortField,
  FileSortDirection,
  FileSearchFilters,
} from '@coexist/wisp-core/types/FileChannelView.types';
